{
  "name": "record-js",
  "version": "0.0.4",
  "description": "JavaScript implementation of a universal blockchain OS",
  "main": "index.js",
  "scripts": {
    "prepublish": "npm run clean && typings install && npm run build && cp -Rn src/* build/",
    "postpublish": "npm run deploy",
    "test": "npm run dist && node --harmony test/index.js && ts-node test/index.js && bash test/index.sh && npm run lint",
    "lint": "npm run tslint",
    "tslint": "find src -name '*.ts' -print0 | xargs -0 -n1 tslint",
    "console": "node --harmony bin/console.js",
    "build": "tsc",
    "dist": "npm run build && webpack --optimize-minimize build/index.js dist/index.js",
    "clean": "rm -rf build && rm -rf dist",
    "deploy": "npm run dist && gh-pages -d dist -m \"Deploy $(npm view record-js version)\"",
    "debug": "devtool test/index.js"
  },
  "bin": {
    "record": "bin/record.js"
  },
  "typings": "build/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/checle/record-js.git"
  },
  "author": "Filip Dalüge <daluege@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/checle/record-js/issues"
  },
  "homepage": "https://github.com/checle/record-js#readme",
  "devDependencies": {
    "devtool": "^2.0.3",
    "gh-pages": "^0.11.0",
    "tape": "^4.6.0",
    "ts-node": "^1.3.0",
    "tslint": "^3.15.1",
    "tslint-config-standard": "^1.5.0",
    "typescript": "^2.0.3",
    "typings": "^1.4.0",
    "webpack": "^1.13.1"
  },
  "dependencies": {
    "bashful": "^1.8.0",
    "browser-vm": "*",
    "fork.js": "^2.0.2",
    "zone.js": "git+https://github.com/checle/zone.js.git"
  }
}

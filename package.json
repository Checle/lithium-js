{
  "name": "record-js",
  "version": "0.0.2",
  "description": "JavaScript implementation of a universal blockchain OS",
  "main": "dist/r.js",
  "scripts": {
    "prepublish": "npm run dist",
    "postpublish": "npm run deploy",
    "postinstall": "typings install",
    "test": "npm run dist && node test/index.js && npm run lint",
    "lint": "find src/ -name '*.ts' -print0 | xargs -0 -n1 eslint",
    "console": "node bin/console.js",
    "dist": "rm -rf dist && tsc",
    "build": "npm run dist && rm -rf build && webpack --optimize-minimize dist/r.js build/r.js",
    "deploy": "npm run build && gh-pages -d build -m \"Deploy $(npm view record-js version)\""
  },
  "bin": {
    "record": "bin/record.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/checle/record-js.git"
  },
  "author": "Filip Dalüge <daluege@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/checle/record-js/issues"
  },
  "homepage": "https://github.com/checle/record-js#readme",
  "devDependencies": {
    "devtool": "^2.0.3",
    "eslint": "^3.3.1",
    "eslint-config-standard": "^5.3.5",
    "eslint-plugin-promise": "^2.0.1",
    "eslint-plugin-standard": "^2.0.0",
    "gh-pages": "^0.11.0",
    "tape": "^4.6.0",
    "typescript": "^1.8.10",
    "typescript-eslint-parser": "^0.1.2",
    "typings": "^1.3.2",
    "webpack": "^1.13.1"
  }
}

import TreeState from './state/tree'

const base = new TreeState()

//base.record('/usr/bin/js', js)
//base.record('#!', bash)
//base.record(0, block)
//base.record(device) // Input falls back to device, local directories are appended to existing record paths

export default base
